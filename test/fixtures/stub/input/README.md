# Stub Eleventy site

This README is not tested, it's here to document the stub Eleventy site struction.

Test runs will generate files in `test/stub/input/test-*.njk`, then render them and compare the output to `test/stub/output/test-*.html`.

See `test/integration.js`